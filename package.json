{
  "name": "observiq-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd observiq-dashboard && npm install",
    "build": "cd observiq-dashboard && npm run build",
    "start": "cd observiq-dashboard && npm start",
    "dev": "cd observiq-dashboard && npm run dev"
  }
}